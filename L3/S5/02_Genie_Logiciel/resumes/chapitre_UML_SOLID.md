<div dir="rtl" align="right">

# ملخص شامل في هندسة البرمجيات (Génie Logiciel)
> **التركيز: UML و SOLID - موجه للامتحانات**
> السنة الثالثة ليسانس - إعلام آلي - جامعة الوادي

---

====================
## **مقدمة (INTRODUCTION)**
====================

### **ما هي هندسة البرمجيات (Génie Logiciel)؟**
هي فرع من فروع الإعلام الآلي يهتم بتطبيق مبادئ وأساليب هندسية منظمة لإنتاج، تطوير، وصيانة البرمجيات. الهدف هو الحصول على برمجيات ذات جودة عالية، قابلة للصيانة، وموثوقة، مع احترام التكلفة والوقت.

### **أهمية UML و SOLID في جودة البرمجيات**
لتحقيق أهداف هندسة البرمجيات، نعتمد على أدوات ونماذج ومبادئ أساسية. من أهمها:

- **UML (Unified Modeling Language)**: هي لغة نمذجة رسومية موحدة تُستخدم لتصور، تحديد، بناء، وتوثيق مكونات نظام برمجي. إنها بمثابة "المخطط الهندسي" للبرنامج، تسمح لنا بفهم وتصميم النظام قبل كتابة الكود.

- **مبادئ SOLID**: هي خمسة مبادئ أساسية في البرمجة الكائنية (Object-Oriented Programming) تهدف إلى جعل تصميم البرمجيات أكثر قابلية للفهم، للتوسيع، وللصيانة. تطبيق هذه المبادئ يقلل من التعقيد ويمنع المشاكل المستقبلية.

---

====================
## **الجزء الأول: مخططات UML (PART 1: UML)**
====================

### **1) مخطط حالات الاستخدام (Use Case Diagram)**

#### **1.1) تعريف مختصر والهدف**
هو مخطط **سلوكي (comportemental)** يصف الوظائف التي يقدمها النظام من وجهة نظر المستخدم. الهدف هو تحديد "ماذا" سيفعل النظام، وليس "كيف". إنه يحدد حدود النظام والمستخدمين المتفاعلين معه.

#### **1.2) مرحلة الاستخدام**
يُستخدم بشكل أساسي في مرحلة **التحليل (Analyse)** لجمع وتحديد متطلبات النظام (Requirements).

#### **1.3) الرموز المستعملة**

| الرمز | الاسم (Français/English) | المعنى |
|:------|:-------------------------|:-------|
| 🧍 | **Acteur** / Actor | مستخدم أو نظام خارجي يتفاعل مع النظام. |
|  oval | **Cas d'utilisation** / Use Case | وظيفة أو خدمة يقدمها النظام. |
| ↔️ | **Association** | علاقة تواصل بين ممثل وحالة استخدام. |
| ▭ | **Système** / System | حدود النظام التي تحتوي على حالات الاستخدام. |
| «include» | **Inclusion** | علاقة "إجبارية". حالة استخدام تستدعي أخرى بشكل دائم. |
| «extend» | **Extension** | علاقة "اختيارية". حالة استخدام تضيف وظيفة لأخرى في شرط معين. |
|  generalization | **Généralisation** | علاقة وراثة بين الممثلين أو حالات الاستخدام. |

#### **1.4) كيف ترسمه يدوياً في الامتحان**
1.  **حدد حدود النظام**: ارسم مستطيلاً كبيراً يمثل النظام.
2.  **حدد الممثلين (Acteurs)**: ارسم الممثلين خارج المستطيل (على اليسار عادةً).
3.  **حدد حالات الاستخدام (Use Cases)**: ارسم الحالات الرئيسية داخل المستطيل على شكل بيضاوي.
4.  **اربط الممثلين بالحالات**: صل بخط (Association) بين كل ممثل والحالات التي يتفاعل معها.
5.  **أضف علاقات «include» و «extend»**: إذا كانت هناك حالة استخدام تعتمد على أخرى، استخدم سهم متقطع مع كلمة «include» أو «extend».

**أخطاء شائعة**: الخلط بين «include» و «extend» (تذكر: include إجباري، extend اختياري)، رسم تفاصيل "كيف" تتم الوظيفة.

#### **1.5) مثال بسيط: نظام تسجيل طالب في الجامعة**
- **Acteurs**: الطالب (Étudiant)، موظف الإدارة (Agent de scolarité).
- **Use Cases**: تسجيل الدخول (S'authentifier)، اختيار المقاييس (Choisir les modules)، دفع الرسوم (Payer les frais)، طباعة شهادة التسجيل (Imprimer le certificat).
- **Relations**: 
    - `اختيار المقاييس` «include» `تسجيل الدخول`.
    - `دفع الرسوم` «include» `تسجيل الدخول`.
    - `طباعة شهادة التسجيل` «extend» `دفع الرسوم` (لا يمكن طباعة الشهادة إلا بعد الدفع).

#### **1.6) ملاحظات هامة للامتحان**
- ركز على الوظائف الرئيسية.
- أسماء حالات الاستخدام يجب أن تكون على شكل فعل (e.g., "تسجيل الدخول" وليس "دخول").
- لا تبالغ في استخدام «extend»، استخدمها فقط للشروط الواضحة.

---

### **2) مخطط النشاط (Activity Diagram)**

#### **2.1) تعريف مختصر والهدف**
هو مخطط **سلوكي (comportemental)** يصف تدفق العمل (workflow) خطوة بخطوة. الهدف هو توضيح تسلسل الأنشطة والقرارات والإجراءات المتوازية داخل عملية معينة أو حالة استخدام.

#### **2.2) مرحلة الاستخدام**
يُستخدم في مرحلة **التحليل (Analyse)** لتفصيل حالة استخدام معقدة، أو في مرحلة **التصميم (Conception)** لوصف منطق عملية (méthode).

#### **2.3) الرموز المستعملة**

| الرمز | الاسم (Français/English) | المعنى |
|:------|:-------------------------|:-------|
| ⚫️ | **État initial** / Initial State | نقطة بداية التدفق. |
| 👁 | **État final** / Final State | نقطة نهاية التدفق. |
| rounded rectangle | **Activité** / Activity | خطوة أو إجراء يتم تنفيذه. |
| ◊ | **Décision** / Decision | نقطة تفرع بناءً على شرط (if/else). |
| ▬ | **Barre de synchronisation** / Fork/Join | لبدء (Fork) أو إنهاء (Join) أنشطة متوازية. |
| → | **Transition** | سهم يوضح الانتقال من نشاط لآخر. |

#### **2.4) كيف ترسمه يدوياً في الامتحان**
1.  **ابدأ بنقطة البداية**: ارسم دائرة سوداء صغيرة.
2.  **أضف النشاط الأول**: ارسم مستطيلاً بحواف دائرية واكتب النشاط بداخله.
3.  **تابع تدفق العمل**: استخدم الأسهم للانتقال بين الأنشطة.
4.  **استخدم المعين (losange) للقرارات**: ارسم معين عند وجود شرط، واخرج منه بسهمين على الأقل مع كتابة الشرط (e.g., `[validé]`, `[non validé]`).
5.  **استخدم الشريط الأسود للتوازي**: إذا كان يمكن تنفيذ نشاطين أو أكثر في نفس الوقت، استخدم شريط (Fork) للخروج منه بأسهم متعددة، ثم استخدم شريط (Join) لجمعها مرة أخرى.
6.  **أنهِ التدفق بنقطة النهاية**: ارسم دائرة سوداء محاطة بدائرة أخرى.

**أخطاء شائعة**: نسيان الشروط على أسهم الخروج من القرار، عدم استخدام Fork/Join للإجراءات المتوازية.

#### **2.5) مثال بسيط: سحب المال من صراف آلي (ATM)**
1.  (Start) → إدخال البطاقة
2.  → إدخال الرقم السري
3.  → (Decision: هل الرقم صحيح؟)
4.  → `[صحيح]` → إدخال المبلغ
5.  → (Decision: هل الرصيد كافٍ؟)
6.  → `[كافٍ]` → صرف المبلغ → إخراج البطاقة → (End)
7.  → `[غير كافٍ]` → عرض رسالة خطأ → إخراج البطاقة → (End)
8.  → `[غير صحيح]` → عرض رسالة خطأ → إخراج البطاقة → (End)

#### **2.6) ملاحظات هامة للامتحان**
- كل مسار في المخطط يجب أن ينتهي بنقطة نهاية.
- يمكن استخدام "Swimlanes" (ممرات السباحة) لتوضيح من يقوم بكل نشاط (e.g., lane للعميل, lane للنظام).

---

### **3) مخطط الحالة (State Machine / Transition Diagram)**

#### **3.1) تعريف مختصر والهدف**
هو مخطط **سلوكي (comportemental)** يصف الحالات المختلفة التي يمكن أن يمر بها كائن واحد (objet) خلال دورة حياته، والأحداث (events) التي تسبب الانتقال من حالة إلى أخرى.

#### **3.2) مرحلة الاستخدام**
يُستخدم في مرحلة **التصميم (Conception)** لوصف سلوك كائن معقد له حالات متغيرة (e.g., طلبية، حساب بنكي).

#### **3.3) الرموز المستعملة**

| الرمز | الاسم (Français/English) | المعنى |
|:------|:-------------------------|:-------|
| ⚫️ | **État initial** / Initial State | الحالة التي يبدأ بها الكائن. |
| 👁 | **État final** / Final State | الحالة التي تنتهي بها حياة الكائن. |
| rounded rectangle | **État** / State | حالة مستقرة للكائن ينتظر فيها حدثاً. |
| → | **Transition** | انتقال من حالة لأخرى. |
| text on arrow | **Événement [Condition] / Action** | صيغة الانتقال: الحدث الذي ي déclenche الانتقال، شرط إضافي، والإجراء المنفذ. |

#### **3.4) كيف ترسمه يدوياً في الامتحان**
1.  **حدد الكائن**: اختر كائناً واحداً تريد دراسة حالاته.
2.  **ابدأ بالحالة الأولية**: ارسم دائرة سوداء وسهم يشير إلى الحالة الأولى.
3.  **ارسم الحالات**: ارسم كل حالة في مستطيل بحواف دائرية.
4.  **ارسم الانتقالات**: صل بين الحالات بأسهم. فوق كل سهم، اكتب الحدث الذي يسبب الانتقال.
5.  **أضف التفاصيل**: يمكن إضافة شروط أو إجراءات على الانتقال (e.g., `boutonPayer cliqué [montantValide] / effectuerPaiement()`).
6.  **حدد الحالة النهائية** (إن وجدت).

**أخطاء شائعة**: وصف سلوك النظام بأكمله بدلاً من كائن واحد، الخلط بين الحالة (state) والنشاط (activity).

#### **2.5) مثال بسيط: حالة طلبية (Commande) في متجر إلكتروني**
1.  (Start) → **`En attente`** (في الانتظار)
2.  `payer()` → **`Payée`** (مدفوعة)
3.  `expédier()` → **`Expédiée`** (تم شحنها)
4.  `livrer()` → **`Livrée`** (تم تسليمها)
5.  `annuler()` من حالة `En attente` → **`Annulée`** (ملغاة) → (End)
6.  `retourner()` من حالة `Livrée` → **`Retournée`** (مرتجعة)

#### **2.6) ملاحظات هامة للامتحان**
- المخطط يركز على كائن واحد فقط.
- اسم الحالة هو اسم (e.g., `Payée`) وليس فعل.
- الحدث الذي يكتب على السهم هو ما يسبب التغيير.

---

### **4) مخطط التسلسل (Sequence Diagram)**

#### **4.1) تعريف مختصر والهدف**
هو مخطط **تفاعلي (interaction)** يركز على **ترتيب الرسائل (messages)** المتبادلة بين الكائنات (objets) مع مرور **الوقت**. الهدف هو توضيح سيناريو معين وكيفية تعاون الكائنات لإنجازه.

#### **4.2) مرحلة الاستخدام**
يُستخدم في مرحلة **التصميم (Conception)** لتفصيل سيناريو معين من حالة استخدام.

#### **4.3) الرموز المستعملة**

| الرمز | الاسم (Français/English) | المعنى |
|:------|:-------------------------|:-------|
| ▭ | **Objet** / Object | كائن مشارك في التفاعل. يكتب اسمه مع خط تحته (`:ClassName`). |
| | | **Ligne de vie** / Lifeline | خط متقطع عمودي يمثل حياة الكائن. |
| █ | **Activation** | مستطيل رفيع على خط الحياة يمثل فترة نشاط الكائن. |
| → | **Message synchrone** | رسالة تتطلب انتظار الرد. |
| ⇢ | **Message de retour** / Reply | رد على رسالة متزامنة. |
| > | **Message asynchrone** | رسالة لا تتطلب انتظار الرد. |
| ⧰ | **Fragment** (e.g., `alt`, `opt`, `loop`) | إطار لتجميع أجزاء من التفاعل (بدائل، شروط، حلقات). |

#### **4.4) كيف ترسمه يدوياً في الامتحان**
1.  **ارسم الكائنات**: ارسم الكائنات المشاركة في السيناريو في الأعلى.
2.  **ارسم خطوط الحياة**: ارسم خطاً متقطعاً عمودياً تحت كل كائن.
3.  **ابدأ التفاعل**: ارسم سهماً أفقياً من الكائن المرسِل إلى المستقبل. الوقت يتدفق من الأعلى للأسفل.
4.  **أظهر فترة النشاط**: عند استقبال رسالة، ارسم مستطيل التفعيل (Activation) على خط حياة الكائن المستقبل.
5.  **استخدم الرسائل المناسبة**: سهم مملوء للرسائل المتزامنة، وسهم متقطع للرد.
6.  **استخدم Fragments**: لتوضيح الشروط (`alt` لـ if/else)، الخيارات (`opt` لـ if)، أو الحلقات (`loop`).

**أخطاء شائعة**: رسم أسهم مائلة (يجب أن تكون أفقية)، نسيان أسهم العودة (Reply) للرسائل المتزامنة.

#### **4.5) مثال بسيط: تسجيل الدخول (Login)**
- **Objets**: `:Utilisateur`, `:InterfaceLogin`, `:ControleurLogin`, `:BaseDeDonnées`
1.  `Utilisateur` → `saisirInfos(user, pass)` → `:InterfaceLogin`
2.  `:InterfaceLogin` → `verifier(user, pass)` → `:ControleurLogin`
3.  `:ControleurLogin` → `findUser(user, pass)` → `:BaseDeDonnées`
4.  `:BaseDeDonnées` ⇢ `userFound` ⇢ `:ControleurLogin`
5.  `:ControleurLogin` ⇢ `résultat` ⇢ `:InterfaceLogin`
6.  `:InterfaceLogin` → `afficherMessage(résultat)` → `:Utilisateur`

#### **4.6) ملاحظات هامة للامتحان**
- المخطط يصف سيناريو واحد فقط (e.g., سيناريو النجاح أو سيناريو الفشل).
- الترتيب العمودي للرسائل هو أهم شيء.

---

### **5) مخطط الأصناف (Class Diagram)**

#### **5.1) تعريف مختصر والهدف**
هو مخطط **هيكلي (statique)** يصف بنية النظام من خلال عرض الأصناف (classes)، خصائصها (attributes)، عملياتها (methods)، والعلاقات بينها. إنه أهم مخطط في UML.

#### **5.2) مرحلة الاستخدام**
يُستخدم في مرحلة **التصميم (Conception)** ويعتبر أساس الكود المصدري.

#### **5.3) الرموز المستعملة**

| الرمز | الاسم (Français/English) | المعنى |
|:------|:-------------------------|:-------|
| ▭ | **Classe** / Class | مستطيل مقسم لـ 3 أجزاء: الاسم، الخصائص، العمليات. |
| `+` `-` `#` | **Visibilité** | `+` (public), `-` (private), `#` (protected). |
| ─ | **Association** | علاقة "يعرف". كائن يعرف بوجود كائن آخر. |
| ─> | **Association dirigée** | علاقة في اتجاه واحد. |
| ◇─ | **Agrégation** / Aggregation | علاقة "جزء-من-كل" غير قوية. الجزء يمكن أن يعيش بدون الكل. (e.g., القسم والطلاب). |
| ◆─ | **Composition** | علاقة "جزء-من-كل" قوية. الجزء لا يمكن أن يعيش بدون الكل. (e.g., السيارة والمحرك). |
| ▷─ | **Généralisation** / Inheritance | علاقة وراثة (is-a). |
| ╌> | **Réalisation** / Realization | صنف يطبق واجهة (interface). |
| `1`, `*`, `0..1` | **Multiplicité** / Cardinality | عدد الكائنات المشاركة في العلاقة. |

#### **5.4) كيف ترسمه يدوياً في الامتحان**
1.  **حدد الأصناف الرئيسية**: ارسم مستطيلاً لكل صنف.
2.  **املأ كل صنف**: اكتب الاسم في الأعلى، ثم الخصائص (مع النوع والرؤية)، ثم العمليات (مع المعاملات والنوع المرجع).
3.  **ارسم العلاقات**: صل بين الأصناف حسب علاقاتها (association, aggregation, composition, inheritance).
4.  **أضف التعددية (Multiplicité)**: على طرفي كل علاقة، اكتب عدد الكائنات المرتبطة (e.g., `1`--`*` تعني أن كائناً من الصنف الأول يرتبط بعدة كائنات من الصنف الثاني).

**أخطاء شائعة**: الخلط بين Aggregation و Composition، نسيان التعددية، عدم تحديد الرؤية (+, -, #).

#### **4.5) مثال بسيط: نظام جامعة**
- **Classes**: `Etudiant`, `Module`, `Professeur`
- **Relations**:
    - `Professeur` `1` -- `*` `Module` (Association: أستاذ يدرّس عدة مقاييس).
    - `Etudiant` `*` -- `*` `Module` (Association: طالب يسجل في عدة مقاييس، والمقياس له عدة طلاب).
    - `Personne` ◁─ `Etudiant` (Inheritance: الطالب هو شخص).
    - `Personne` ◁─ `Professeur` (Inheritance: الأستاذ هو شخص).

#### **4.6) ملاحظات هامة للامتحان**
- هذا المخطط هو الأكثر أهمية وتفصيلاً.
- انتبه جيداً للفرق بين Composition (الماسة المملوءة) و Aggregation (الماسة الفارغة).
- التعددية ضرورية جداً.

---

====================
## **الجزء الثاني: مبادئ SOLID (PART 2: SOLID)**
====================

### **مقدمة**

#### **ما هي أنماط التصميم (Design Patterns)؟**
هي حلول عامة وقابلة لإعادة الاستخدام لمشاكل شائعة ومتكررة في تصميم البرمجيات. إنها ليست كوداً جاهزاً، بل هي وصف أو قالب لكيفية حل مشكلة معينة. أمثلة: Singleton, Factory, Observer.

#### **الفرق بين SOLID و Design Patterns**
- **SOLID**: هي **مبادئ** عالية المستوى (high-level principles) تخبرنا بخصائص التصميم الجيد. إنها قواعد إرشادية تساعدنا على تجنب التصميم السيء. هي "لماذا" نفعل شيئاً.
- **Design Patterns**: هي **حلول** منخفضة المستوى (low-level solutions) لمشاكل محددة. هي "كيف" نطبق التصميم الجيد. العديد من أنماط التصميم مبنية على مبادئ SOLID.

---

### **S: Single Responsibility Principle (SRP)**

1.  **التعريف الرسمي**: "A class should have only one reason to change." - يجب أن يكون للصنف سبب واحد فقط للتغيير.
2.  **الفكرة المبسطة**: كل صنف يجب أن يكون مسؤولاً عن شيء واحد فقط.
3.  **مثال (Java-style)**:

    *   **Bad**: صنف `Employe` يقوم بحساب الراتب وحفظ بيانات الموظف في قاعدة البيانات.
        ```java
        class Employe {
            public double calculerSalaire() { /*...*/ }
            public void sauvegarderEmploye() { /*...*/ }
        }
        ```
        (له سببان للتغيير: تغيير في طريقة حساب الراتب، أو تغيير في قاعدة البيانات).

    *   **Good**: نفصل المسؤوليات.
        ```java
        class Employe { /*...*/ }
        class CalculateurSalaire {
            public double calculer(Employe e) { /*...*/ }
        }
        class EmployeRepository {
            public void sauvegarder(Employe e) { /*...*/ }
        }
        ```

4.  **أسئلة الامتحان**: "هل هذا الصنف يحترم SRP؟ لماذا؟"، "أعد تصميم هذا الصنف ليحترم SRP". الكلمات المفتاحية: **مسؤولية (Responsibility)**, **سبب للتغيير (Reason to change)**.

---

### **O: Open/Closed Principle (OCP)**

1.  **التعريف الرسمي**: "Software entities (classes, modules, functions) should be open for extension, but closed for modification." - يجب أن تكون الكيانات البرمجية مفتوحة للتوسيع، ومغلقة للتعديل.
2.  **الفكرة المبسطة**: يجب أن تتمكن من إضافة وظائف جديدة دون تعديل الكود الموجود.
3.  **مثال (Java-style)**:

    *   **Bad**: صنف يحسب مساحة الأشكال باستخدام `if/else`.
        ```java
        class CalculateurAire {
            public double calculer(Object forme) {
                if (forme instanceof Rectangle) { /*...*/ }
                if (forme instanceof Cercle) { /*...*/ }
            }
        }
        ```
        (لإضافة شكل جديد `Triangle`، يجب تعديل هذا الصنف).

    *   **Good**: نستخدم الوراثة والـ Polymorphism.
        ```java
        interface Forme {
            double aire();
        }
        class Rectangle implements Forme { public double aire() { /*...*/ } }
        class Cercle implements Forme { public double aire() { /*...*/ } }
        
        // لا نحتاج لتعديل هذا الصنف لإضافة أشكال جديدة
        class CalculateurAire {
            public double calculer(Forme f) {
                return f.aire();
            }
        }
        ```

4.  **أسئلة الامتحان**: "كيف يمكننا إضافة وظيفة جديدة دون تعديل الكود؟"، "هل هذا التصميم يحترم OCP؟". الكلمات المفتاحية: **توسيع (Extension)**, **تعديل (Modification)**, **if/else أو switch على الأنواع**.

---

### **L: Liskov Substitution Principle (LSP)**

1.  **التعريف الرسمي**: "Subtypes must be substitutable for their base types." - يجب أن تكون الأصناف المشتقة قابلة للاستبدال مكان أصنافها الأساسية دون التأثير على صحة البرنامج.
2.  **الفكرة المبسطة**: إذا كان `S` هو صنف فرعي من `T`، فيجب أن أتمكن من استخدام كائن من النوع `S` في أي مكان يتوقع كائناً من النوع `T`.
3.  **مثال (Java-style)**:

    *   **Bad**: صنف `Rectangle` له `setWidth` و `setHeight`. الصنف `Carre` يرث منه. عند استدعاء `setHeight` على مربع، يجب أن يغير العرض أيضاً، وهذا يغير سلوك الصنف الأب.
        ```java
        void useRectangle(Rectangle r) {
            r.setWidth(5);
            r.setHeight(10);
            assert r.getWidth() == 5; // ينجح للمستطيل، يفشل للمربع!
        }
        ```

    *   **Good**: لا نجعل `Carre` يرث من `Rectangle`. يمكن أن يرثا كلاهما من صنف `Forme` مجرد.

4.  **أسئلة الامتحان**: "هل الوراثة هنا صحيحة؟"، "ماذا يحدث لو مررنا كائناً من الصنف الابن لهذه الدالة؟". الكلمات المفتاحية: **وراثة (Inheritance)**, **استبدال (Substitution)**, **سلوك غير متوقع (Unexpected behavior)**.

---

### **I: Interface Segregation Principle (ISP)**

1.  **التعريف الرسمي**: "Clients should not be forced to depend on methods they do not use." - لا يجب إجبار العملاء على الاعتماد على واجهات (interfaces) لا يستخدمونها.
2.  **الفكرة المبسطة**: الواجهات الكبيرة (Fat interfaces) سيئة. من الأفضل أن تكون لديك عدة واجهات صغيرة ومحددة بدلاً من واجهة واحدة كبيرة وعامة.
3.  **مثال (Java-style)**:

    *   **Bad**: واجهة `Worker` تحتوي على `work()` و `eat()`.
        ```java
        interface IWorker {
            void work();
            void eat();
        }
        class Human implements IWorker { /* ok */ }
        class Robot implements IWorker {
            public void work() { /* ok */ }
            public void eat() { /* ??? الروبوت لا يأكل! */ }
        }
        ```

    *   **Good**: نفصل الواجهات.
        ```java
        interface IWorkable { void work(); }
        interface IFeedable { void eat(); }
        
        class Human implements IWorkable, IFeedable { /* ok */ }
        class Robot implements IWorkable { /* ok */ }
        ```

4.  **أسئلة الامتحان**: "ما هي مشكلة هذه الواجهة؟"، "أعد تصميم هذه الواجهات لتحترم ISP". الكلمات المفتاحية: **واجهة (Interface)**, **إجبار (Forced)**, **فصل (Segregation)**.

---

### **D: Dependency Inversion Principle (DIP)**

1.  **التعريف الرسمي**: "High-level modules should not depend on low-level modules. Both should depend on abstractions. Abstractions should not depend on details. Details should depend on abstractions." - الوحدات عالية المستوى لا يجب أن تعتمد على الوحدات منخفضة المستوى. كلاهما يجب أن يعتمد على التجريد (abstractions). التجريد لا يجب أن يعتمد على التفاصيل. التفاصيل يجب أن تعتمد على التجريد.
2.  **الفكرة المبسطة**: لا تعتمد على الأصناف الملموسة (concrete classes)، بل اعتمد على الواجهات (interfaces) أو الأصناف المجردة (abstract classes).
3.  **مثال (Java-style)**:

    *   **Bad**: صنف `Notification` يعتمد مباشرة على `Email`.
        ```java
        class Email {
            public void send() { /*...*/ }
        }
        class Notification {
            private Email email = new Email();
            public void sendNotification() {
                email.send();
            }
        }
        ```
        (لتغيير الإشعار إلى SMS، يجب تعديل صنف `Notification`).

    *   **Good**: نعتمد على واجهة.
        ```java
        interface IMessageSender {
            void send();
        }
        class Email implements IMessageSender { /*...*/ }
        class Sms implements IMessageSender { /*...*/ }
        
        class Notification {
            private IMessageSender sender;
            public Notification(IMessageSender sender) { // Dependency Injection
                this.sender = sender;
            }
            public void sendNotification() {
                sender.send();
            }
        }
        ```

4.  **أسئلة الامتحان**: "كيف يمكن جعل هذا الصنف أكثر مرونة؟"، "ماذا لو أردنا تغيير ...؟". الكلمات المفتاحية: **اعتمادية (Dependency)**, **تجريد (Abstraction)**, **قلب (Inversion)**, **مرونة (Flexibility)**.

---

====================
## **UML و SOLID معاً (UML & SOLID TOGETHER)**
====================

### **كيف يساعد UML (خاصة مخطط الأصناف) في تطبيق SOLID؟**
مخطط الأصناف هو الأداة المثالية لتصميم وتصور تطبيق مبادئ SOLID قبل كتابة الكود:
- **SRP**: في مخطط الأصناف، إذا رأيت صنفاً له عدد كبير جداً من العمليات (methods) التي لا تبدو مترابطة، فهذا مؤشر على انتهاك SRP. التصميم الجيد يظهر أصنافاً صغيرة ومركزة.
- **OCP**: يمكن تمثيل OCP باستخدام الوراثة (Generalization) والواجهات (Realization). عندما ترى أن التصميم يعتمد على واجهات بدلاً من أصناف ملموسة، فهذا يشير إلى احترام OCP.
- **LSP**: علاقة الوراثة في مخطط الأصناف يجب أن تكون منطقية. إذا كانت العلاقة "is-a" غير صحيحة (مثل مثال المربع والمستطيل)، فإن المخطط يكشف هذا الخلل في التصميم.
- **ISP**: إذا رأيت صنفاً يطبق واجهة ولكنه لا يحتاج لكل عملياتها، يمكنك إعادة تصميم ذلك في مخطط الأصناف عن طريق فصل الواجهة الكبيرة إلى واجهات أصغر.
- **DIP**: في مخطط الأصناف، يجب أن ترى الأسهم (الاعتماديات) تشير من الأصناف الملموسة نحو الواجهات أو الأصناف المجردة، وليس العكس. هذا هو "قلب الاعتمادية" مصوراً.

### **أخطاء تصميم شائعة تؤدي لانتهاك SOLID**
- **الصنف الإله (God Class)**: صنف واحد يعرف كل شيء ويفعل كل شيء. (ينتهك SRP).
- **الاعتماديات الصلبة (Hard-coded Dependencies)**: استخدام `new` لإنشاء كائنات داخل صنف آخر بدلاً من تمريرها من الخارج. (ينتهك DIP).
- **الوراثة من أجل إعادة استخدام الكود فقط**: وراثة صنف فقط للاستفادة من بعض عملياته، حتى لو لم تكن العلاقة منطقية. (ينتهك LSP).

---

====================
## **ورقة الغش للامتحان (EXAM CHEATSHEET)**
====================

### **UML Diagrams**
- **Use Case**: ماذا يفعل النظام من وجهة نظر المستخدم (وظائف).
- **Activity**: كيف يتدفق العمل خطوة بخطوة داخل عملية واحدة (workflow).
- **State**: كيف تتغير حالة كائن واحد عبر الزمن (دورة حياة).
- **Sequence**: كيف تتفاعل عدة كائنات معاً في سيناريو معين (رسائل + وقت).
- **Class**: كيف يبدو هيكل الكود (أصناف + علاقات).

### **SOLID Principles**
- **SRP**: كل صنف له مسؤولية واحدة فقط.
- **OCP**: أضف الجديد دون تعديل القديم (استخدم الوراثة والواجهات).
- **LSP**: الابن يجب أن يتصرف تماماً مثل الأب (الوراثة يجب أن تكون منطقية).
- **ISP**: واجهات صغيرة ومحددة أفضل من واجهة واحدة كبيرة.
- **DIP**: لا تعتمد على التفاصيل، اعتمد على التجريد (استخدم الواجهات).

---

====================
## **تعلم لغوي (LANGUAGE LEARNING)**
====================

### **جدول المصطلحات**

| العربية | Français | English |
|:--------|:-----------|:----------|
| ممثل | Acteur | Actor |
| حالة استخدام | Cas d'utilisation | Use Case |
| علاقة | Association | Association |
| وراثة | Généralisation / Héritage | Inheritance |
| تجميع | Agrégation | Aggregation |
| تركيب | Composition | Composition |
| تعددية | Multiplicité | Multiplicity |
| واجهة | Interface | Interface |
| صنف مجرد | Classe abstraite | Abstract Class |
| رسالة | Message | Message |
| تجريد | Abstraction | Abstraction |
| مسؤولية | Responsabilité | Responsibility |
| اعتمادية | Dépendance | Dependency |
| نمط تصميم | Patron de conception | Design Pattern |
| كيان | Entité | Entity |

### **جمل أكاديمية شائعة**
- **FR**: "Ce diagramme montre l'interaction entre les objets..."
- **EN**: "This diagram shows the interaction between the objects..."

- **FR**: "La classe A hérite de la classe B."
- **EN**: "Class A inherits from Class B."

- **FR**: "Pour respecter le principe OCP, on utilise une interface..."
- **EN**: "To respect the OCP principle, we use an interface..."

---

====================
## **تمارين (PRACTICE)**
====================

### **1) تمرين UML**
**المطلوب**: ارسم مخطط الأصناف (Class Diagram) لنظام حجز فندقي مبسط.

**المعطيات**:
- الفندق له عدة غرف.
- الغرفة لها رقم ونوع (فردية، مزدوجة).
- العميل يمكنه إجراء عدة حجوزات.
- كل حجز يخص عميلاً واحداً وغرفة واحدة فقط، وله تاريخ بداية ونهاية.

**إرشادات للحل**: 
1.  حدد الأصناف: `Hotel`, `Chambre`, `Client`, `Reservation`.
2.  فكر في العلاقات: هل هي association, aggregation, أم composition؟
    - ما العلاقة بين `Hotel` و `Chambre`؟ (إذا دمر الفندق، تدمر الغرف -> Composition).
    - ما العلاقة بين `Client` و `Reservation`؟ (Association).
3.  حدد التعددية (Multiplicité) لكل علاقة.
    - `Hotel` `1` -- `*` `Chambre`.
    - `Client` `1` -- `*` `Reservation`.
    - `Chambre` `1` -- `*` `Reservation`.

### **2) تمرين SOLID**
**المطلوب**: حدد مبدأ SOLID الذي تم انتهاكه في الكود التالي، واقترح حلاً.

```java
// نظام لتصدير التقارير
class ReportExporter {
    public void exportToPdf(Report r) {
        // code to export to PDF
    }
    
    public void exportToWord(Report r) {
        // code to export to Word
    }
}
```

**تلميح**: ماذا لو أردنا إضافة التصدير إلى Excel؟ هل سنقوم بتعديل صنف `ReportExporter`؟ أي مبدأ يمنع هذا؟

**الحل**: تم انتهاك مبدأ **OCP (Open/Closed Principle)**. الحل هو استخدام واجهة `IReportExporter` مع أصناف منفصلة لكل نوع تصدير (`PdfExporter`, `WordExporter`, `ExcelExporter`).

</div>
